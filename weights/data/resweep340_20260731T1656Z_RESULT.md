# RESULT — validation item `resweep-340`, scored against the stake in this directory

    stake      : weights/data/resweep340_20260731T1656Z_STAKE.md  (written BEFORE any cell ran)
    tree       : worktree-law5-prefill @ 8c65172 (v1.24.0, unpushed). No tracked file modified.
    grid       : 10 models x 17 machines x ctx{0,16384} = 340 cells @ 2.5 bits, unselected.
                 As staked. K-D did not fire.
    nothing timed, no GPU, no cal_id: pure arithmetic over the shipped preset tables.

## 1. The two PASS criteria

    A-1  invented upgrade pairs (two FIRED upgrades, DIFFERENT resources, SAME tok/s)  ......  0
         live population: 24 cells where two different-resource upgrades both fire. Not vacuous.
    A-2  counterfactual-identity mismatches, caught at the evaluate() boundary  .............  0
         700 counterfactual calls inspected; every one carried the baseline's n_layer,
         true_size_gb, t, a, ne, moe, bits, kvp, codebook_share, gl, ctx, vc, vb, geta and
         differed in exactly the one resource key its upgrade names (or in nothing, when the
         override coincides with the machine's own value).
    A-3  advisor tok/s + winning row not reproduced by an independent recomputation  ........  0
         131 fired upgrades, all reproduced to 1e-9.
    B-1  UNREACHABLE speculation headlines printed unqualified  ............................  0
    B-2  qualifier printed on a row that could reach the headline  .........................  0
    B-3  printed bound != independently recomputed bound  ..................................  0
    B-4  row terms failing to reconstruct their own tok/s  .................................  0

    VERDICT vs K-A / K-B: PASS.

## 2. Falsification (K-C) — the auditor CAN fail

    --inject upgrade  (strip n_layer/true_size_gb inside upgrade_advisor only, i.e. the pre-fix
                       call sites)                              EXIT 1
                      A-1 10 invented pairs | A-2 700/700 mismatches | A-3 51 arithmetic misses
                      fired upgrades rise 131 -> 158.
                      It reproduces the historical signature EXACTLY:
                        llama-70b/colibri/ctx16384  +16 GB RAM 0.5749513  ==  NVMe SSD 0.5749513
                      matching the committed 0782477 artifact's `old` column to 6 decimals.
    --inject spec     (speculation_advice(..., row=None), the pre-C-15 call site)   EXIT 1
                      B-1 22 unreachable headlines printed unqualified.

    Both injections fire. K-C did not trip.

## 3. The previously-affected cells, INDIVIDUALLY (full table: *_cells.log)

    D-2, the 17 cells whose headline was unreachable: 17/17 now print
         "NOT REACHABLE ON THIS ROW" with the row's own bound. 0 unmeasurable.
         Bounds run 1.025x - 1.229x against a 2.10x headline, CPU-attention share 64.5% - 95.3%.
    D-2b 5 FURTHER cells are oversold by the shipped gate that the old sweep's `ceiling(k=2)<1.5`
         metric did not list (llama-70b on 2016-xmp / 2016 / gaming / laptop-8gb / rtx-3060 at
         ctx16384). All 5 carry the qualifier. The shipped gate is the stricter of the two.
    D-1, the 31 cells whose upgrade advice moved: 31/31 clean, and every one prints EXACTLY the
         post-fix advice the 0782477 artifact recorded — bit-for-bit through six later plan.py
         commits. 0 unmeasurable.

## 4. Staked predictions scored

    P-1 invented pairs = 0                                          HIT
    P-2 counterfactual identity, 0 mismatches                       HIT
    P-3 advisor arithmetic reproduces                               HIT
    P-4 unreachable headlines = 0                                   HIT
    P-5a >=15 of 17 still oversold, 100% qualified                  HIT (17/17 oversold, 17/17 qualified)
    P-5b 0 of 31 shows an invented pair                             HIT
    P-6 both injections exit non-zero                               HIT
    P-7 exp54's own K-3 will FIRE against 0782477^                  **MISS — published here at
        equal prominence.** K-3 PASSED: 0 of 340 staked-grid cells and 0 of 340 pinned-size /
        codebook replays moved. I predicted six later commits had moved predicted numbers; they
        had not. Re-running exp54 on the current tree reproduces the 0782477 artifact EXACTLY:
        class counts 178/99/33/30, modal share 52.35%, 127 spec cells, 17 oversold, 31 upgrade
        changes (same cell set, same post-fix advice), 137 all-in-VRAM winners, 1527 recon rows.
        Driver verdict: PASS, all five kill rules held.
        The reason the prediction was wrong, stated rather than argued away: K-3 compares row
        NAME, tok/s and FLAGS of the winner. 03203bc's `-ub 4096` removal touches dense splits,
        which win no cell of this grid at 2.5 bits; ac6b8e1 changed capacity_probe, which is a
        probe and not a row; 8db0199 changed a row's WARN string, which K-3 does not compare.

## 5. What could NOT be measured — say it plainly

  * THE 4.7x HEADLINE ITSELF WAS NEVER EXERCISED BY THIS SWEEP. 25 of the 127 speculation cells
    are on the MoE `split experts` branch that quotes 4.7x, and ALL 25 have cpu_compute EXACTLY
    0, so bound == R exactly and the qualifier is correctly silent. It is silent by CONSTRUCTION,
    not by luck: only three placements in `evaluate` carry a cpu_compute term — the dense split,
    `pure CPU (GPU idle)` and `stream from disk (cold experts)` — and none of them is a
    `split experts` placement, because on that placement attention stays on the GPU. So on the
    shipped preset grid the 4.7x branch CANNOT be oversold through this mechanism.
    All 22 qualified cells are the DENSE 2.10x headline.
    The 4.7x branch's gate is nevertheless LIVE, verified directly rather than assumed: a
    synthetic `split experts` row carrying 50% CPU attention makes speculation_advice print
    "NOT REACHABLE ON THIS ROW ... at most **1.65x**", and the same row without a CPU term
    prints no qualifier. The code path works; this grid just cannot reach it.
    => "zero unreachable 4.7x headlines" is TRUE but is evidence about the 2.10x branch.
       The 4.7x branch is covered by a synthetic probe only.
  * NO BORDERLINE POPULATION. Zero of the 127 cells sit between `bound < R` and the shipped 1%
    display tolerance, so SPEC_HEADLINE_TOL is doing no load-bearing work on this grid and this
    run says nothing about whether 1% is the right number.
  * A DISPLAY-ROUNDING NOTE, not a failure: SPEC_X_NGRAM_TUNED = 4.634146 is printed as "4.7x"
    (+1.42%); SPEC_X_NGRAM_DENSE = 2.101695 is printed as "2.10x" (-0.08%). On a row with zero
    CPU attention the arithmetic ceiling is 4.634, so the printed digit is 1.4% above the
    constant even in the best case. That is a 2-significant-figure rendering of 98.8/21.3, not a
    reachability defect, and it is disclosed rather than folded into a tolerance.
  * NOT MEASURED AT ALL: whether the advice is CORRECT about hardware. This sweep proves the
    counterfactual is drawn from the right row menu and the headline is bounded by the row's own
    terms. It does not prove that buying the recommended RAM delivers the printed tok/s. That
    needs an A/B on real hardware and is not in scope here.

## 6. Artifacts (none committed)

    resweep340_20260731T1656Z_STAKE.md            the stake, written first
    resweep340_20260731T1656Z_audit.json/.log     clean run, every cell
    resweep340_20260731T1656Z_inject_upgrade.*    falsification arm 1 (exit 1)
    resweep340_20260731T1656Z_inject_spec.*       falsification arm 2 (exit 1)
    resweep340_20260731T1656Z_cells.log           the 17 + 5 + 31 cells, one line each
    resweep340_20260731T1656Z_exp54.json/.log     driver sweep re-run vs baseline 0782477^
    weights/resweep340_audit.py                   the auditor (untracked)
    weights/resweep340_confirm_cells.py           the per-cell reporter (untracked)

    The tracked exp54_binding_constraint.{json,log} and exp54_plan_head.py.txt were restored with
    `git checkout --` after the driver run; `git status` shows no modified tracked file.
